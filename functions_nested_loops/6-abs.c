#include "main.h"
/**
 * _abs - Return the absoltue value
 * Description: if postive return it if negative X-1 and return it
 * @n : Any number
 * Return: The absoulte value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-1 * n);
	}
	return (n);
}
